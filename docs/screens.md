# 画面仕様（クラシックシングルス・ランクマッチ）

サンプル: `samples/2026-08-20 10-44-35.mp4`（OBS録画・1920x1080・11分47秒・3試合）
検証日: 2026-08-20。DB正解と照合済み。座標はすべて 1920x1080 基準（4Kは×2で自動スケール）。

## 1試合のシーケンス

```
[対戦相手を探しています] → [しばらくおまちください] → [黒/地球儀]
→ [VS画面] ≈2秒 ★played_at基準・コート/キャラ/名前
→ [イントロ演出(小バナー)] → [ウォームアップラリー(0-0)] → [試合]
→ (ゲーム毎に中央バナー「名前 n - m 名前」)
→ [MATCH POINT] → [コイン演出] → [Winner + スコアパネル] ≈2-4秒 ★スコア/勝敗
→ [しばらくおまちください] → [レートパネル] 8秒〜 ★mode/レート前後
→ (メニュー表示のまま滞留 or 次のマッチングへ)
```

## シーン判定（192x108縮小フレーム・vision.js classify）

| シーン | 条件 |
|---|---|
| vs | 中央VS白(90-103,89-100)>0.25 が前提。①自分カード青(66-81,89-96)>0.15 かつ 相手カード橙(111-126,89-96)>0.10 ②片側が薄いとき（青>0.15かつ橙>0.02 ／ 青≤0.15かつ橙>0.10）は 左黒帯(16-60,91-98)>0.45 かつ 右黒帯(132-176,91-98)>0.28 で救済 ※カード窓はカード実寸（1920換算 x663-815 / y887-960）に合わせること。外すと巨体キャラのアイコンに覆われた側が 0.000 に潰れる（2026-09-07 ブンブン×ドンキーコングで試合が丸ごと消えた） |
| winner | 黄ハイライト(160-176,78-98 両行域)>0.06 かつ パネル暗色(148-173,77-98)>0.25 ※パネルは半透明で、フォレスト等明るいコートでは暗色率が0.3程度まで下がる。勝者行は上下どちらもありうる |
| rating | 中央パネル明色低彩度(70-122,32-62)>0.60 |

- winner窓は2〜4秒と短い → スキャン1秒刻み・1ヒットでも採用
- 緑のロード画面/キャラ選択/イントロ小バナーはVSと誤検出しないことを負例10種で確認済み

## 抽出領域（FHD座標）

| 項目 | 画面 | 領域 | 方式 |
|---|---|---|---|
| コート名 | VS左上 | (35,28,460,70) | 領域NCC照合（コート別参照画像） |
| 自分キャラ | VS左カード | (700,898,90,88) | アイコンNCC |
| 相手キャラ | VS右カード | (1148,898,90,88) | アイコンNCC |
| 相手名 | VS右 | (1255,895,410,85) | v0=画像を見て手入力（将来OCR） |
| シングルス/ダブルス | レートパネル左上 | (655,295,205,60) | 領域NCC |
| フィーバーラケット あり/なし | レートパネル右上 | (1000,298,285,50) | 領域NCC（なし=classic） |
| レーティング | レートパネル | (1040,548,235,70) | fillinkマスク→グリフ分割→テンプレ照合 |
| 世界ランキング | レートパネル | (1055,428,220,95) | 同上（CSVには不使用・テンプレ収穫用） |
| スコア行1 | Winnerパネル | icon(1480,795,75,75) digit(1598,783,145,100) | 黄=勝者行。行とキャラはアイコンで対応付け |
| スコア行2 | Winnerパネル | icon(1480,890,75,75) digit(1598,882,145,98) | |

## 数字フォントの読み方（重要な知見）

- レート数字は**縁取り同士が接触**するため、暗い縁取り（darkink）では分割不能
- **縁取り内側の白い塗り（fillink: 輝度>215 かつ 彩度<15）**なら数字ごとに孤立 → 列射影で分割可
- **分割は連結成分(CC)ベース**（列射影は不可）。理由: ①コンマが数字の懐に横位置で重なり縦の谷では分離不能 ②プロポーショナルで縁取り同士が接触。塗り同士は連結しないのでCCで確実に分かれる
- CC前に**縦3px未満の画素を除去**（パネル縁の細い水平線がグリフ同士を橋渡しして1成分に融合するため）
- エロージョン後の数字の塗りの実高はクロップ高の**約36%**（旧45pxは細線混入の嵩増しだった）。フィルタ: 数字=高さ25〜85%・コンマ=高さ30%以下かつ下半分・右端細片と縦光は除去
- 収穫時のラベル対応は**x順でなくクラス別**（数字箱/コンマ箱を形状分類して数字ラベルを数字箱にx順で割当）。コンマは数字の懐に入るためx順が揺れる
- **要注意教訓**: 「期待数に合わせて最も幅広の箱を割る」ヒューリスティックは、融合箱が普通の数字と同幅のケースで別の箱を割り**ラベルがズレてテンプレートが汚染**される。形状パターン検証つきの分割のみ許可
- レートパネルはフェードイン中グリフが拡大されて歪む → 数値は「連続2回の読みが一致」した値のみ採用（analyzer.readNumberAt）
- スコアは**デュースで8点以上になりうる**（実例: 8-6）。勝者スコア=7固定の仮定は禁止。Winnerパネルの勝者行も固定でない（上下どちらもある）
- レートパネルの数値は**カウントアップ演出**: 表示直後=rating_before → 2〜4秒後に安定=rating_after
  - before = 窓先頭+0.4/+0.9秒の2回読みで一致確認
  - after = 窓末尾-1.0/-0.4秒の2回読みで一致確認

## played_at

- 録画ファイル名から開始時刻（OBS: `YYYY-MM-DD HH-MM-SS`、Switch: 16桁数字）+ VS画面の動画内時刻
- DBの登録実績と数十秒精度で一致することを確認済み

## テンプレート収穫状況（この動画から）

- レートフォント: 0,1,3,4,5,6,7,8,9,コンマ,位 —— **「2」だけ未収穫**（この動画に出現しない）
- Winnerパネルフォント: 7(黄), 1, 2, 5(灰)
- VSアイコン: ハナチャン, ノコノコ:赤, ゲッソー, パタパタ
- パネルアイコン: 同4種
- コート: アカデミー サンド
- mode: シングルス / なし
- → 未知グリフ・未知アイコンは低スコアになり「要確認」フラグでレビューUIに回す設計。動画が増えるたび収穫を重ねて拡充する

## 既知の注意点

- ブラウザの`<video>`は長時間・大量シークで**フレームが更新されなくなる**ことがある（seekedは発火するのに絵が古いまま）
  → src再設定で復旧。本番アプリではフレームのハッシュ監視で自動復旧を入れる
- Winner画面の行順は今のところ「自分が上」だが、負け試合のサンプル未確認 → アイコンで行を対応付ける実装にした
- 敗北時のレート画面/Winner画面のサンプルが未収穫（今後の録画で要確認）
- ミラー戦（同キャラ対戦）はアイコンでの行対応付けが効かない → Winner名テキストで判定する将来課題


## 回線品質アイコン（2026-08-28実測）

- 4本バーの階段アイコン・**1試合に1つ＝対戦全体の回線品質**。黄色=点灯・悪いとグレー化・リアルタイム変動
- 位置(FHD): クラシック(S/D共通)=左上 x43-80,y25-70 ／ フィーバー=HPバー右 x373-415,y30-75（やや斜め）
- 読み: connClassic/connFever領域で黄色ピクセルの「列ラン」本数=点灯本数（readConnection）。
  HUD非表示（しばらくおまちください・カメラ演出）はok:falseで棄却→試合中7サンプルの平均を採用

## レートボードの仕様メモ（2026-08-28 ユーザー直伝）

- 「レーティング」ラベルの右の数値は**必ず4桁・桁区切りカンマ付き**（現行の4桁固定読みと整合）
- 数値の下の**黄色いバーは到達度**。カンスト状態では満タンのまま**長さが変わらない**
  → レート読みが不安定なときの裏取り材料候補（バー長が変わらない=カンスト帯、など。未実装）
- ボード上部の表示は現行の mode1/mode2 領域そのもの: **左=シングルス/ダブルス・右=フィーバーラケットあり/なし（クラシック）**
  （部門判定はここから読んでおり実戦精度も高い）
