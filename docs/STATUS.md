# SENSEKI SCAN 実装ステータス（2026-09-07時点）

次セッションへの引き継ぎ用サマリー。**このファイルを最初に読めば全体像が掴める**ように保つ。
分野別の詳細は各ドキュメントへ（末尾の索引）。

## 1. 本体アプリ（app/・Electron）
**バージョン: v1.0.1公開済み（2026-09-06 02:5x・FVゲージside帰属の根本修正＋トランジション既定0.3秒＋配信元 games-desu・Latest）／ v1.0.0公開済み（2026-09-05 19:4x 発行し直し・初公開は 1.0.0 表記にしたいというユーザー要望で、1.0.1の内容を 1.0.0 として再ビルド→ v1.0.1 と旧 v1.0.0 のリリース/タグは削除。verify-release 両側OK・Latest）／ v0.3.16公開済み（2026-09-02・バナーのリボン判定を色非依存に＋ゲーム画面の位置指定。v0.3.15=ダブルス解析のクラッシュ修正）**

### v1.0.4候補（2026-09-07・利用者フィードバック2件）
- **「CSVをコピー」を押しても反応が分からない** → コピー成功で押したボタンが2.5秒「コピーしました」に変わり、画面下中央にトースト（試合数＋次の手順）。
  失敗時は赤トースト＋「CSVを保存」への案内。コピー自体もメインプロセスの `clipboard.writeText`（IPC `copy-text`）を第一に使う
  （レンダラーの navigator.clipboard はウィンドウ非フォーカスで失敗する）→ navigator.clipboard → execCommand の順にフォールバック。
  samples/frames/patch-copyfeedback-0907.js で適用。devフォールバック（Browser pane）で成功/失敗の両経路を確認済み
- **キャサリン（自分側）→チコ:黒 の誤認報告**: 報告元はネヤさん（X @neya700）のクラシックダブルス18試合（2026-09-06 22:08〜23:15・23:48にSCAN経由で一括登録・DB上は全部キャサリンに直っている）と推定。
  辞書の状態: dblIcons のキャサリンは **相手側(橙)1本だけ**（ユーザー辞書エクスポート由来・目視収穫ではない）／vsIcons は青1・橙1（互いの輝度NCC 0.781＝ポーズ差が大きい）／チコ:黒は dblIcons 青1・vsIcons 青7。
  辞書だけの模擬実験（samples/frames/exp-kath-0907.js＝カード色セルを反対側の色に置換して matchIconCard）では片側のみ56本すべて正解（キャサリン模擬青→キャサリン0.975）で**再現せず**。
  実フレームは無い（キャサリンが映った 2026-08-25/26 の録画は削除済み）ので、実運用の失敗は「1本しか無い手札とポーズが違う＋青カード」の複合とみるのが妥当。
  **対処＝自分側(青・上の行/下の行の両方)のキャサリンをCOMダブルスで録画して収穫**（ロゼッタ自分側5本と同じ型・収穫は icon-harvest-c2.js の型）。
  **→ 収穫済み（2026-09-07 00:16・samples/2026-09-07 00-12-13.mp4 = ユーザーのCOMダブルス・自分=キャサリン(L1・青・上の行)/味方=チコ:黒(COM)/相手=ワンワン・キャサリン(R2・橙)）**:
  収穫前の本番照合(matchIconCardAround)は L1 キャサリン 0.783（正解だが要確認）・R2 0.854。新手札の輝度NCCで**最も近い別キャラはチコ:黒 0.38**＝報告どおりの取り違え先。
  sweep-d.js の __harvestDbl で L1/R2 を 0.2秒刻み16点採取→静止アイコンなので dedupe 後 青1+橙1（harvest-0907-kath.json）→ merge-0905.js で両 templates.json へ（dblIcons 100→102・キャサリン3本）。
  再照合: t=3.0/4.6/6.2 とも L1 キャサリン 0.999〜1.000・L2 チコ:黒 1.000・R2 キャサリン 0.999〜1.000。旧1本（ユーザー辞書由来）は新2本と輝度0.42〜0.48しか似ていない（ズーム違いの別ポーズ・目視ではキャサリン）ので残置。
  COM戦は名前欄がキャラ名表示になるので myname 学習には使わない（ユーザー了承済み）
  回帰（headless-analyze・14-52-01 ダブルス5試合）: 5試合・キャラ20体・スコア・コート・レート連鎖(2613→…→2611)すべて9/6の結果と同一。要確認は自分の行(名前未学習)のみ＝辞書追加の副作用なし。
  リリースノート docs/release-notes-1.0.4.md / -short.md を用意（改番・ビルドは未実施）

### v1.0.3候補（2026-09-06・利用者報告「配信レイアウトの録画で検出0」＝根因は自分側アイコンによる青カード隠れ）
利用者（ぴんや・X経由）の報告: 1080p配信をYouTube Studioから720pでDL→「ゲーム画面の位置」自動検出(X9 Y67 幅936 高527)で解析しても VS 0件・299秒の孤児レート警告。
- **切り分け（新ツール `node tools/headless-analyze.js <video> [--rect x,y,w,h]`）**: headless Chrome で本番 index.html を iframe に読み込み analyzeFile() をそのまま走らせ、
  解析ログ・CSV・要確認フラグを標準出力に出す。利用者と**同一のログを再現**できた（Chrome拡張経由のタブは裏に隠れていると video が読み込まれず使えない）。
  `--debug` で Chrome コンソールも表示。「ゲーム画面の位置」は localStorage の sc_settings 経由で渡す（index.html の SETTINGS/matches は let なので外から触れない→結果は buildCsvText() で受け取る）
- **根因＝位置ずれでも720pでもなく VS判定の青条件**: VS画面(173〜175秒)は白VSロゴ0.30・相手オレンジ0.36・左右黒帯0.54は成立するのに、
  自分カードの青が **0.000**（ブンブンの巨体アイコンが青カード領域 x670-780/y910-980 をほぼ全部覆う。実寸でも青2.2%）。
  相手側には既に「茶色系キャラで橙が0.04に落ちる→黒帯で救済」があったが自分側には無かった。**手持ち 15-18-15 の自分クッパ(青0.04)も同じ理由で見逃していた**
  （STATUSに「録画開始前に始まっていた試合」と書いたのは誤診。VSは t=7〜9 に映っている）。
- **修正 vision.js classify**: 白VSロゴ>0.25 を前提に「青>0.15かつ橙>0.10」／「青>0.15かつ橙>0.02＋黒帯」／**「青≤0.15かつ橙>0.10＋黒帯」（新設）**。
  1fps全編走査で手持ち9本5,069フレーム（samples/*.mp4＋利用者動画）を旧/新比較 → 偽陽性0・回復2件（利用者ブンブン173-175秒・15-18-15クッパ7-9秒）。
  色を見ない案（白＋黒帯のみ）は 01-44-03 で新規1件出たので採用せず。
- **修正後の再現（利用者動画・936x527）**: 1試合 classic_singles ブンブン vs クッパJr. 7-3 win 3082→3091 スタジアム グラス 回線4 ＝全項目が画面と一致。
  要確認は 自分キャラ（辞書のブンブンは1本のみ）・レート前後（横1280px未満）・日時 の4件＝正解だが要確認、で妥当
- ついでに parseStartTime に和文ファイル名 `2026年09月04日11時59分` を追加（source=name・要確認なしになる）
- ブンブン自分側/クッパJr.相手側の収穫は**不要と決定（ユーザー判断 2026-09-06・元動画がYouTube DLの720pで画質が低く辞書に入れたくない）**。要確認が付くのは想定どおり
- 回帰（headless-analyze・修正後）: **15-18-15 は 3→4試合**（m1 クッパ vs マリオ:オーバーオール 1-7 loss 3375→3370 が新たに成立・以後3試合は従来どおり）／
  21-24-23 は従来どおり1試合（ベビィルイージ vs ノコノコ 4-7・トゲゾー/ビリキュー・3149→3110）／利用者動画は和文ファイル名から 2026-09-04 12:01 が入り要確認3件に減少
- **v1.0.3 公開済み（2026-09-06 19:1x・https://github.com/games-desu/senseki-scan/releases/tag/v1.0.3・verify-release ローカル/リモートともOK・docs/release-notes-1.0.3.md）**。
  ビルドは Claude が `npm run dist` を **サンドボックス無効(dangerouslyDisableSandbox)で実行して成功**（NSIS はサンドボックス内では動かないが外せば通る。ユーザーが出先でも発行できる）

### v0.4.0候補（2026-09-02・ハイライト生成＝得点シーンの自動切り抜き・**詳細は docs/highlight.md**）
ユーザー要望: ①サーブ開始〜得点 ②得点直前の数秒 を自動で切り抜き、手直しでき、②は試合ごとのダイジェストにも。
**X投稿用にプレイヤー名が映る画面（ポイント間スコアバナー・VS・勝敗）は必ずカット。**
- 認識: Phase A（tools/rally.js のHUD消灯方式）をアプリに同梱（renderer/rally.js）＋ highlight.js
  （試合窓→0.5秒走査→ポイント化→二分探索＋後ろから手前への確認パスで消灯を決める→終端は消灯−0.033秒→書き出し前の名前ガード）。
  **実機で +0.05 は名前バナーが映った**（二分探索がステイルフレームで遅れる側に外れる・ヘッドレスでは再現しない）→ confirmOff 追加。
  名前バナーはHUD消灯の **+0.117秒で完全一定**（60fps実測40か所・全モード/解像度）、最終ポイントの勝敗パネルは +3.8秒
- ヘッドレス検証 `tools/highlight-node.js`（renderer をそのまま vm で実行）: 砂クラシック 7-1/7-2/7-5、
  フィーバー 4-7/2-7/7-4、ダブルス 6-8(デュース)/7-3 を全ポイント検出・偽ポイント0
- 見つけた根因2件: **砂コートは消灯中も7スロットが黄に読める→readSide に fill≥0.25 を追加**（tools/rally.js にも反映）／
  数字未描画の一瞬で前後比較が切れる→「直前に読めた状態」との比較に変更
- 書き出し: `ffmpeg-static` 同梱（asarUnpack・predist で存在確認）。-ss を -i の前＋libx264 veryfast crf18 で
  フレーム正確・5秒クリップ1.4秒。ダイジェストは concat copy。gameRect があれば crop、1440p以上は1080p縮小
- UI: hl-ui.js（パネル・試合ごとの一覧・区間調整モーダル＝プレビュー/タイムライン緑=HUD/赤=名前・書き出し3種・進捗/中止）
- 実機確認: 区間調整モーダルまで動作（2026-09-02・フィーバーダブルス・草コート）。**書き出しも実機で確認済み**（2026-09-05・ユーザーがダイジェストを作成しバッジの見え方を指摘＝保存まで動いている）
- 2026-09-02 追加要望対応: 始点のVS残像（fresh＝イントロ直後は始点+1.0秒）／UIを番号付きの流れに
  （1.区間の種類=得点前N秒・ラリー全体・試合ごと → 2.録画 → 3.チェック → 4.保存形式=区間ごと・1本に繋げる）／
  走査 1.0秒刻みで解析時間ほぼ半減（精度維持を10-44-35で確認）
- 実機: 22試合247ポイント(990秒)の解析成功。「区間ごとに保存」が無反応に見えた件→進捗を保存ボタン横にも表示＋
  名前ガードを自動区間は末尾/先頭のみに軽量化。編集モーダルに前/次ポイント移動を追加
- 自分のキャラ表示バッジ（VS画面から照合・試合ヘッダで選び直し・canvas→透過PNG→overlay・最初のN秒/ずっと）
- ダイジェストのつなぎ目にトランジション（xfade/acrossfade・既定ディゾルブ0.5秒・10本ずつ段階連結）。ffmpeg 呼び出しは app/hl-ffmpeg.js へ分離（build.files に追加済み）
- 残: ②始点を打点に合わせる（Phase C 待ち）・1ポイント目の①にウォームアップが含まれる
- 2026-09-05 公開前の仕上げ（ユーザー指摘）:
  - **トップ画面を「1. 戦績を抽出する」主役に再構成**: ドロップ領域に見出し＋「録画ファイルを選ぶ…」ボタン（緑の破線枠・パネル自体も緑枠）、
    相手名自動入力とゲーム画面の位置は1行の設定行へ、**ハイライト生成は `<details>` で畳んだ「2.」**（既定は閉。直近の録画があれば見出しに
    「直近の録画（…）で作れます」を出す）。ドロップ領域の状態文言は `#dropMsg` に出す（`drop.textContent` 直書きは廃止）
  - **「使い方」を新機能込みで書き直し**: 1.戦績抽出（ドロップ→解析→1件ずつ確認→CSVをコピー→クリップボードから読み込む）／
    2.ハイライト生成（区間の種類→録画→チェック・区間の調整→保存形式）／こまったとき（何も検出されない・バグ報告）
  - **バッジの切り出しをカード内に修正**: R.myicon(下30%がカード外)をそのまま使っていたため顔が上に寄り下に黄色いラインが入っていた
    → `{x:698,y:890,w:96,h:66}`（実測カード y888〜958）。3キャラで顔が中央に来ることを確認

### v1.0.2 公開済み（2026-09-06 15:55・games-desu/senseki-scan・verify-release OK・docs/release-notes-1.0.2.md）
内容=カード色非依存照合＋ダブルス行ずれ許容＋勝敗行の色違い同一視＋自分の行セレクタ＋名前8つ保持＋録画の開始日時＋辞書追加E＋コード整理。次=v1.0.3候補: 録画開始前に始まっていた試合(VS無し)の検出・ガボン自分側収穫

#### （旧見出し）v1.0.2候補（2026-09-06・package.json改番済み）: キャラ照合をカード背景色に非依存に（ガボン→ヨッシー:ピンク誤認の根本対策）
- **利用者フィードバック①（2026-09-06）ロゼッタ→ベビィピーチ**: ガボンと同じ「片側の手札しか無い」パターン（ロゼッタは相手側2枚のみ・ベビィピーチは自分側3枚のみ・生RGBの類似は0.07＝似ていない）。
  新照合(matchIconCard)では輝度NCCでロゼッタの最寄り別キャラがベビィピーチ0.68＝**辞書内で最も近いペア**なので、確実にするには自分側ロゼッタの手札を1枚入れたい（COM戦でVS画面が映れば十分）
- **利用者フィードバック②（2026-09-06）対戦日時のずれ**: YouTubeアーカイブをDLした動画は更新日時=DL時刻。対策を実装:
  parseStartTime が {date, source} を返し、ファイル名の日時（OBS「2026-09-05 21-24-23」・Switch「20260905_212423」・区切り _ / : / T も）を最優先。
  日時が読めない/日付だけのときは更新日時から推定して **played_at を要確認**にし、ログで警告。結果の上に「録画の開始日時」パネル
  （動画ごとに datetime-local + 適用）を追加し、適用でその動画の全試合の played_at を「開始日時＋試合開始位置(m._vsT0)」で作り直す（手修正=青枠）。
  検証: 同じ動画を OBS 名と archive_download.mp4 の2回解析 → 前者は日時◯/フラグ無し、後者は推定値+要確認+警告 → 21:24 を適用で 21:24:xx に更新・フラグ解除・edited。
  samples/frames/patch-starttime-0906.js で適用。マニュアル・使い方に追記
- **利用者フィードバック③（2026-09-06・X @neya700）ダブルスで自分の名前の位置が左上/左下で固定でない**: 指摘どおり。
  従来は「シングルスVS画面から学習した自分の名前」で行を決め、未学習なら**同梱テンプレ(開発者の名前)と照合**＝他の利用者では当てずっぽうだった
  （ダブルスだけ・古い動画から始めた人が該当）。対策を実装（samples/frames/patch-myrow-0906.js）:
  - 解析時に左列2行ぶんの材料（キャラ照合結果・ベクトル・ズーム・名前画像/sig/band）を m._dbl に持ち、applyMyRow(m,row) で自分/味方を割り当てる。
    未学習なら上の行を仮置きして **flags.myRow（要確認「自分の行」）**＋ログで案内。同梱 myname テンプレは両 templates.json から削除（照合コードも廃止）
  - カード（1件ずつ/一覧とも）に「自分の行」セレクタ（上の行が自分／下の行が自分・名前画像つき・未判定は赤枠）。押すと chooseMyRow: その試合に当てはめ→
    選んだ行の名前を learnMySig で学習→同じ解析中の未判定ダブルス試合に myRowFromSigs を再適用（当てはまった数をログ）。判定済みでも押し直し可（手修正=青枠）
  - ハイライト側（hl-ui.js）の行ラジオでも選んだ行の名前を学習（w.my.sigs を保持）
  - 検証: 12-53-12.mp4（COM同士・名前=キャラ名）のVSから擬似試合3件→「下が自分」で自分/味方入れ替え・MYSIG 1枚学習・「上が自分」に戻せる。
    伝播は m2 の下段に m1 上段の名前sigを複製して検証→m2 が下段に自動で切り替わり decided、m3 は要確認のまま（期待どおり）
- **追加収穫E（2026-09-06・samples/2026-09-06 14-45-08.mp4 = ロゼッタ/ダブルスVS ＋ 2026-09-06_02-28-45.mp4 = 利用者の動画・YouTube DL品質）**:
  - 14-45-08: シングルスVS（ロゼッタ vs ロゼッタ COM・t≈21）→ vsIcons ロゼッタ **自分側**を収穫（5本）。ダブルスVS（t≈143・ゲッソー/ワルイージ vs パタパタ/クリボー）
    → dblIcons ゲッソー(青カード)1本（ワルイージ/クリボーは既存と0.98以上で重複・R1パタパタは色不明なので除外）。勝敗パネル無し（メニュー画面が winner に誤分類されるだけ）
  - 02-28-45: 利用者(ゆあん=カロン vs yxxki=ゲッソー・アカデミー サンド)。vsIcons カロン(自分側)1本・ゲッソー(相手側)3本・panelIcons ゲッソー/カロン 各1本
  - ホールドアウト（収穫前の辞書で照合）で**利用者の2件を再現**: 自分側ロゼッタ → 旧照合=ベビィピーチ0.648／新照合(matchIconCard)=ロゼッタ0.892。
    青カードのゲッソー(ダブルス) → 旧=ヨッシー:青0.646／新=ゲッソー0.961。つまり v1.0.2 の照合だけでも直り、収穫で0.999に。
    利用者動画（ソフトな画質）は新照合の輝度スコアが 0.67〜0.70 と低め（旧0.81〜0.83）＝要確認は付くが正解。辞書に入ったので同品質は以後1.0
  - 辞書: vsIcons 307→**316本**・panelIcons 134→**136本**・dblIcons 99→**100本**（いずれも64種）。総当たり(自分除外)で高スコアの誤ラベルは色違い同士のみ
- **ダブルス実戦の精度チェック（2026-09-06・samples/2026-09-06 14-52-01.mp4 ランクマ クラシックダブルス5試合 18分27秒）**: 自分の名前は21-24-23のシングルスVSから学習した状態で実行。
  5試合すべて検出・自分の行(1,2,1,2,2)全部正解・キャラ20体全部正解・コート/スコア/勝敗/レート連鎖(2613→2594→2580→2602→2605→2611)/回線すべて証拠画像と一致。
  ただし初回はキャラ照合スコアが 0.54〜0.77 のものが9体あり要確認だらけ（例: ベビィルイージ L1=1.00 なのに L2=0.54、ドンキー R1=1.00/R2=0.74）。
  **原因=行によるアイコン位置ずれ**: 下の行(L2/R2)のアイコンは上の行より約3px左・1px下（±3px探索で best が dx=±3,dy=1 に集中）。辞書は片方の行で採取したものが多い。
  **対策=位置ずれ許容照合** vision.js matchIconCardAround(video, region, iw, ih, lib, opt, {dx:4, dy:2}) を追加し、ダブルスの4アイコンで採用（採用した vec を教える用に保持）。
  再実行: 全20体 0.87〜1.00・要確認0件。シングルス(myicon/oppicon)は3本とも中心で1.00＝ずれ無しなので対象外。
  残る注意: m4 味方はノコノコ(基本)0.98 と ノコノコ:黄 0.95 が入れ替わりうる（甲羅が映らない色違い＝既知の構造的限界・フラグは立てない方針）。samples/frames/patch-dblshift-0906.js
- **クラシックシングルスの精度チェック（2026-09-06・samples/2026-09-06 15-18-15.mp4 ランクマ3試合 11分58秒）**: 3試合検出・キャラ(ヨッシー:オレンジ vs カロン/トッテン/ゲッソー)・コート・スコア(7-3/7-2/7-5)・レート連鎖(3370→3376→3382→3410)・回線すべて証拠画像と一致。
  見つかった問題2件:
  ① m2/m3 の result が要確認: 勝敗パネルのアイコンが ヨッシー:オレンジ→ヨッシー:ピンク(0.96/0.98) に化け、どの行も自キャラに一致せずミラー扱い→レート増減で確定はするがフラグ。
     **対策=行の照合は色違いを同一視**（base名=":"の前で比較。ミラー判定も base で）。再実行で要確認0件を確認
  ② **録画開始前に始まっていた試合(クッパ vs マリオ 1-7・勝敗/ランク画面は映っている)は検出されない**: groupMatches が vs 起点で、途中試合は捨てる仕様。
     マニュアルの「録画の途中から始まった試合も検出します」は誤りだったので「対戦カードが映っていない試合は検出しません」に修正。
     拾うなら: winner+rating だけの組を試合にし、自分の行=勝ちなら勝者行/負けなら敗者行(レート増減で判定)、キャラはパネルアイコン、コートは試合中HUD左上の名前から→**未実装(v1.0.3候補)**
  補足: トッテンのVSアイコンは相手側のみ0.72で要確認(正解・辞書に片側しか無い)。ゲーム画面の位置は既定のまま実行
- **自分の名前を複数保持（2026-09-06・配信者は「ネヤ」「ネヤTwitch」のように名前を変える）**: learnMySig を「0.97以上=同じ名前は末尾へ移す／別名は追加・最大8つ」に変更（旧: 0.995で重複判定・3枚→同じ名前が毎試合積まれ別名が押し出されていた）。
  chooseMyRow で選ばなかった行の名前を forgetMySig(0.9以上を削除)＝味方の行を誤って選んだときの取り消し。検証: 同名別フレーム0.998で1枚のまま・別名2つ追加で3・再学習で末尾へ・両行とも学習済みなら null(選ばせる)→forget で解決
- **同梱のコード整理（2026-09-06・レビュー指摘分・動作不変）**: samples/frames/cleanup-0906.js で一括適用（完全一致置換・26箇所）
  - vision.js: nccLen を廃止し ncc(a,b,n) に比較長を統合／ダブルスVS判定 detectDoublesVs(video) と REGIONS.dMidBand を新設（index.html と hl-ui.js の重複3条件を一本化）
  - index.html: SCENE_CACHE のキー生成を sceneKey(f) に一本化（hl-ui.js も同じ関数）／LAST_FILE 更新時に sc-lastfile イベントを発火／死んだCSS(.hlmy select・.hlfin)削除
  - hl-ui.js: 1秒ポーリング(setInterval)を廃止し refreshUseLast() を sc-lastfile・パネルtoggle・busy切替で呼ぶ／p.idx の O(n²) indexOf を forEach の添字に／ジョブID生成を newJobId() に統一／my フォールバックの死んだ name フィールド削除
  - highlight.js: readFrame(video,{hudOnly}) を追加し、得点走査 scanWindow は HUD だけ読む（classify と readSide×2 を毎サンプル計算しない。名前ガード guardRange は従来どおり全部読む）
  - main.js: 一時フォルダ生成を hlTmpDir() に統合
  - 検証: node --check 全ファイル／tools/highlight-node.js（readFrame 既定動作）8ポイント検出／開発ページで 21-24-23 を解析→全項目正解→直近の録画で作る→11ポイント(4-7と一致・24秒)→区間の調整/前後/キー無視/全部選ぶ/種類切替/一覧/1件ずつ/使い方/バグ報告/ゲーム画面の位置/クリア すべて実行時エラーなし
  - 未着手（影響が大きい・別途）: hlRender の innerHTML 全再構築／サムネ生成の再シーク／xfade 連結の三重エンコード＋probe／findBanner の毎フレームマスク確保
ユーザー報告: 自分側のガボンが「ヨッシー:ピンク」(0.61・要確認フラグ付き)になった。
- 原因: VSアイコンのRGB NCCはカード背景色（自分=青／相手=橙）に支配される。ガボンの手札は相手側2枚のみで、自分側のガボンはどれとも合わず
  低スコア帯の最上位がヨッシー:ピンクだった（ガボン↔ヨッシー:ピンクの類似は0.39＝似ているわけではない）。
  **辞書64キャラ中47キャラが片側の手札しか無い**構造的問題（両側そろえるには全キャラ×両側の収穫が要る）
- 対策: vision.js に matchIconCard(v, lib, {w, rows}) を追加し、index.html のシングルス(vsIcons・16x16の上11行)とダブルス(dblIcons・16x12)の照合に採用。
  ①輝度だけのNCCでキャラ（ベース名）を決める → ②同ベースに色違いがあるときだけ「カード色（左端3列の平均）に近いセルを灰に潰した色つきNCC」で色を決める。
  score は①（要確認 0.8 判定に使う）。辞書ベクトルは全体RGBのまま（再収穫不要・テンプレ側の変換はキャッシュ）
- 評価（辞書の自己照合）: vsIcons 反対側だけで照合 → キャラ正解 99/101（生RGBは全滅級）・top1 94/101／全体LOO 287/299（生RGB 279）／
  別キャラ間の輝度NCC最大 0.71（<0.8＝未収穫キャラは引き続きフラグ）。dblIcons 反対側 3/38→26/38・全体 31/53→41/53
- 実フレーム: 01-44-03 5試合＋21-32-20 2試合＋21-24-23 で新旧とも全部1.00で一致（正解）。
  **片側手札を隠した模擬（ガボン状況の再現）**: 自分側ハナチャン(青手札を除去) 旧 パタパタ:青0.64 → 新 ハナチャン0.82／相手側ゲッソー(橙除去) 旧 ロゼッタ0.61 → 新 ゲッソー0.86／
  ボスパックン 旧0.73 → 新0.79／デイジー 旧 ピーチ0.70 → 新 デイジー0.84
- 残る限界: 色違い（ノコノコ黄→赤 等）は従来どおり。ガボン自分側の手札はユーザーの録画（2026-09-05 23:17の試合）が samples/ に入れば収穫して同梱する
- samples/2026-09-06_02-28-45.mp4（ゆあん vs yxxki・第三者の録画?）は全項目のスコアが低め（コート0.77・キャラ0.83）＝画面位置ずれ系。今回の評価には不使用

### v1.0.1（公開済み 2026-09-06 02:5x）: FVゲージ side帰属の根本修正＋トランジション既定0.3秒＋配信元 games-desu
- 発行 https://github.com/games-desu/senseki-scan/releases/tag/v1.0.1（3点整合OK・Latest）。GitHub/Vercel の名前変更(KawasakiKeitoGames→games-desu 等)の後に再ビルドしたものを発行
ユーザー報告「相手のフィーバーショットが自分のショットと推定された」（21-24-23 ワンダーコート・自分ベビィルイージ/トゲゾー vs ノコノコ/ビリキュー）。
開発用ページの再現では自分/相手とも null（フラグ）になり、原因を掘ると side帰属の土台が2つ壊れていた:
- **FVゲージ領域が左右非対称だった**: HUDは「ストック枡（内側の小さな枡）＋充填バー」。旧 fvL(305-465)はバーだけで枡(216-310)が外、
  旧 fvR(1540-1700)は枡(1605-1701)＋バー端。発動は**枡の消灯**として出るため、自分の発動が落差0.13〜0.21しか出ず閾値0.22に届かなかった
  （相手側は枡込みで0.33〜0.39落ちる）。→ fvL{216,72,250,20}/fvR{1453,72,249,20} に対称化
- **HUD可視ゲート（回線アイコン）が回線品質1〜2で点滅**: 141サンプル中80しか通らず、発動直前のサンプルが欠けてエッジ条件（直前2点）が
  成立しない。→ 「枡とバーの境目の黒い縦罫（fvBorderL/R）が両側とも暗い」ゲートに変更（94サンプル・HUD非表示の誤通過0）。
  半透明フェード中(回線アイコンは見えるがゲージは薄い)も正しく落とす
- エッジ検出: 窓末尾で次点サンプルが無いときも許容（最終ポイントの発動）／落差閾値を dropMin 引数化し既定 0.22→**0.18**
- 検証: 21-24-23 → 自分トゲゾー/相手ビリキュー（全帰属正解）。回帰 01-44-03 m2（GT 自分ファイアバー/相手マジック）→ 名前付き6件中5件帰属・誤り0
  （0.22のままだと相手1件のみで自分null）。**旧コードでは 21-24-23 は両方null＝ユーザー環境の「相手→自分」は前試合からの持ち越し仮入力
  （myRacket carryover）だった可能性が高い**（seenに前試合のラケットがあれば持ち越す仕様）
- 未検証: ワルイージピンボール（旧ゲートが通りにくかったコート）・オフライン録画（回線アイコン無し→新ゲートなら通る見込み）
- 同時に: ハイライトの**トランジション既定を 0.5→0.3秒**（「動画が短く感じる」）。保存済み設定がある人は自分の値のまま
- **samples/ の GT 4本（10-44-35/22-18-37/01-57-16/00-08-01）はユーザーが 2026-09-05 に削除（不要と判断・2026-09-06確認）。**
  docs/rally-probe/*・docs/highlight.md・tools/rally-bench.js 等が参照する時刻はもう再現できない。ラリー解析(Phase C以降)を再開するときは新しい録画でGTを取り直す
- 回帰用に Archive の 2026-08-30 01-44-03.zip を samples/ に展開した（2.7GB・GT: 16:48Z フィーバー ファイアバー/マジック・他クラシック4）

### 公開直後に見つけたハイライトのバグ修正（一度 v1.0.1 として発行→ 19:4x に v1.0.0 へ統合し直し）
- **公開後のバグ確認（2026-09-05・ユーザー依頼「他にもバグがないか確認して」）**: スモークテスト（一覧/1件ずつ/使い方/バグ報告/ゲーム画面の位置/区間の調整/クリア）は実行時エラーなし。
  コードレビュー（8角度・うち4角度はAPI上限で私が直接）で見つけて修正したもの:
  - **ハイライト一覧のスコア強調が一度も効いていなかった**: scoreHtml の正規表現が (d+) - (d+) と書かれていた（バックスラッシュ欠け＝過去のヒアドキュメント潰れ）で
    「動いた側の数字だけ太字」が常に不成立 → 数字クラス（バックスラッシュ付きの d）に修正。レンダラー全体を走査して同種の欠けは他に無し
  - **「区間の調整」モーダル中に ←→/Enter を押すと裏の「1件ずつ確認」が試合送りされていた**（index.html の keydown が hlmodal を見ていなかった）→ モーダル表示中は無視
  - **畳んだ「2. ハイライト生成」の見出しへ録画をドロップすると、読み込んでも中身が見えない** → hlLoad で details を open に
  - 解析完了後もドロップ枠が「解析中: …」のままだった → 「解析済み: N試合（追加の録画はここへドロップ・「クリア」で最初から）」に
  レビューで挙がった整理候補（未対応・v1.0.1には含めない）: nccLen は ncc に長さ引数を足せば1本にできる／ダブルス判定の3条件と中央帯座標が index.html と hl-ui.js に重複／
  SCENE_CACHE のキー生成が2か所／hlUseLast の setInterval 1秒ポーリングはイベント駆動にできる／scanWindow の readFrame が使わない classify/readSide を毎サンプル計算／
  サムネ生成の再シーク／hlRender の innerHTML 全再構築／xfade 連結の三重エンコード＋probe 起動／findBanner の毎フレーム 614KB マスク確保。詳細は 2026-09-05 のセッション
- 2026-09-05 19:28 に v1.0.1 として発行したが、初公開は 1.0.0 表記にしたいという要望で package.json を 1.0.0 に戻して再ビルド。**v1.0.1 と旧 v1.0.0 は gh release delete --cleanup-tag で削除**し、新ビルドを v1.0.0 として発行（https://github.com/games-desu/senseki-scan/releases/tag/v1.0.0）。
  注意: 旧 1.0.0 を入れた端末（開発者本人のみ）は同一バージョンのため自動更新されない→手で上書きインストール。今後の改番は 1.0.1 から
- **「直近の録画で作る」で得点0件になるバグ**: analyze() が SCENE_CACHE に **gateRatings 前**のシーン結果を保存していたため、
  試合中の偽レート窓（砂コート等で30件超出る）が残り、matchWindows が試合開始2秒後に窓を閉じて「0試合・0ポイント」になっていた。
  hlLoad 側の自前スキャン経路は gateRatings 後に保存しているので、ハイライト単独では起きない（＝ユーザーのダイジェスト作成は成功していた）。
  → SCENE_CACHE.set を gateRatings の後へ移動。検証: 10-44-35 で 解析→直近の録画で作る → 窓 25-198/279-464/488-694・3試合29ポイント（修正前は窓2秒・0件）
- 開発用ページの注意: ページ本体の video 要素は非表示ペインでステイルフレームを返すため、gateRatings をページ内で手動再実行しても棄却されない（実アプリでは問題なし）

### 公開告知の準備（2026-09-05）
- リンクまとめサイト（games-desu.vercel.app）に SENSEKI SCAN を **SENSEKI FEVER の直下**に追加（GitHub Releases latest へ・JP/EN・icon-scan.png）。デプロイ済み
- X用スライド4枚（1600x900）: samples/frames/slides/slides.html を headless Chrome で描画（?only=sN で1枚ずつ）。
  実画面のスクショは同一オリジンの iframe ラッパー（shot-top.html）と、解析後DOMのスナップショットHTML（slides_review.html / slides_hl.html）を撮影。
  **相手プレイヤー名は blur で伏せる**（namecrop）。成果物は Documents/Claude/senseki-scan-x-1.0.0/（01〜04.png + post.txt）
- README / docs/manual.md を v1.0.0 のトップ画面（1.戦績を抽出する／2.ハイライト生成）と「CSVをコピー→クリップボードから読み込む」導線に更新

### リリースの回し方（確立済み・2026-08-28）
- ユーザー: 実ターミナルで `cd C:\Users\iftec\Documents\senseki-capture\app; npm run dist`（NSISはサンドボックス不可）
- Claude: `gh release create vX.Y.Z` で発行代行（gh CLI認証済み・exe+blockmap+latest.ymlの3点添付・説明欄は1行=1項目で更新ダイアログにそのまま表示される）
- 公開リポ: https://github.com/games-desu/senseki-scan ／ 手順詳細は `docs/release.md`
- **exe/blockmap/latest.ymlは必ず同じビルドから3点同時に上げる**。exeだけ差し替えると
  全ユーザーが `sha512 checksum mismatch` で更新不能になる(v0.3.11で発生・2026-08-30)。
  公開前後に `node tools/verify-release.js [--local]` で照合すること

### v0.3.7(公開済み 2026-08-29・実戦フィードバック対応)
- コート/ラケットのプルダウンを全候補化(コート=FEVERマスタ18種静的リスト・ラケット31種。カーペットが候補に出なかった)
- レート待機画面フォールバックの走査下限を前試合レートパネル終端に制限
  (クラシック直後のフィーバー戦に前試合のクラシックレート3410が無警告で入った実例)
- collectBannersの見切れガード(帯端接触バナーを照合から除外。「ーブースト」だけの部分バナーがスターと0.92誤マッチ)
- 確認UI: 試合中カード廃止→回線バー証拠クロップ・最終スコア画像縮小
- 辞書収穫: racketBanners 77→80(ビリキュー/ダッシュ/かざん)・vsIcons 93→95(テレサ/チコ:オレンジ)・
  dblIcons 54→60(ピーチ/クッパ両側/パタパタ/トッテン)・courts 46→51(カーペット3種/ワルイージピンボール2種)

### v0.3.8(公開済み 2026-08-29・48Mbps実戦12試合で発見した根因修正+朝のフィードバック対応)
- ランク付きダブルスパネルの部門ボードをmode1辞書に収穫(14→15)。未収穫だと検証ゲートが
  ダブルスのレート窓を全棄却→直後の試合を丸呑みし勝敗/レートを接ぎ木する大事故になる
- bannerOverlapsPanelの誤爆修正: パネル下端〜コートの白帯(y976-1009)がfindBannerに拾われ
  x1>=1400が常時成立→本物の勝敗パネルを3試合分スキップ。スコア数字帯(y821-965)との
  縦オーバーラップを必須化
- 勝敗パネル処理をprocessWinner()関数化+バッチ完了後の後段再探索パス(持続的デコーダ負荷の
  ステイルフレーム保険。同じ検索窓が単独実行では毎回成立する現象を実測)
- 回線サンプリング2.5秒間隔(上限120点)・証拠クロップは本数別(サンプル10%以上観測のみ)・独立行表示
- 検証: 同素材3回目のフル解析で12/12試合・全スコア/全レート取得・3部門のレート連鎖完全整合

### FVゲージside帰属の刷新(2026-08-29午後・d766c94・v0.3.9候補)
- 旧方式(バナー窓ごとの減少量比較)は同時発動で必ず誤る→**ゲージ時系列のエッジ検出方式**へ:
  走査中に{t,L,R}を常時記録・HUD可視ゲート(回線アイコン成立フレームのみ採用。発動中はHUDが
  消えピンボールの市松床が輝度率読みを汚染)・エッジ=相対ドロップ(直前2点min-0.22以上・
  落下先<=0.45。「空」の基準値が左右非対称: 自分0.08/相手0.29のため絶対閾値は不成立)・
  両ゲージ同時エッジ破棄・バナーは最近傍未消費エッジに割り当て(±3.5s)
- ラケット照合閾値0.85→0.93(未収穫ラケットが語尾共通部で0.88-0.90誤マッチ=マジック→フリーズ等)
- GT検証(2動画・side既知23イベント): 正解10/安全null6/誤り1。決定後のラケット値は全て正解orフラグ
  (旧方式は誤り5+)。v2m1=おばけ/ダッシュ両正解・v1m2=ビリキュー/スター両正解・v1m9=相手カーブ正解/自分フラグ

### v0.3.10(公開済み 2026-08-30・確認UIフィールドカード型刷新)
- 全入力欄の真上に根拠画像: [全景サムネ+日時|部門ボード2枚|コート名|回線] [キャラズーム] [名前]
  [ラケットバナー(ハテナは種類別・帰属不明はラベル付き)] [スコア自分行/相手行分割|result] [レート数字前後]
- 解析ログはページ下部の専用パネルへ(「ログ」ボタンで開閉)
- 第3実戦対応: 勝敗クロップの黄色検証・回線本数の構造検証(黄色背景フラッド対策)・
  ゲート警告集約・HUD可視プロキシ緩和・コート僅差フラグ・マメキノコ収穫
- ビルドの既知問題: NSIS工程が1回目に失敗することがある(255KBのexe+中間ファイル残留)→再実行で成功。
  ウイルス対策のスキャン除外(appdist)で安定する見込み

### v0.3.11(公開済み 2026-08-30・パラパラ再生+回線横並び)
- ラケット発動の前後6コマ(コート全体)を自動巡回再生する疑似GIF(SEQ_REG+400ms間隔・クリック拡大)
- 回線クロップ横並び44px(conninline)・発動画像なし側は帰属不明バナー表示

### v0.3.14(公開済み 2026-08-31・名前の文字辞書=実質OCR + ダブルス自分行判定の刷新)
「初対戦の相手でも名前が入る」ようにするため、**ユーザーが入力した名前を教師に文字辞書を育てる**方式を追加。
キャラ/コートの教えるサイクルを文字に適用したもの。

- **学習**: CSV保存時、手入力/修正した名前について、名前画像を**入力された文字数に合わせて分割**し
  1文字ずつ収穫する(userData/chars-user.json・1文字3枚まで・酷似ncc>=0.99は追加しない)。
  分割は「ストローク塊を隙間の狭い順に併合して文字数に合わせる」方式＝ラベルのズレが起きない。
  塊が足りない(文字同士がくっついている)場合は収穫を見送る。実測 22/23プレートで収穫成功
  (見送りは詰まった英大文字 MTZA のみ)
- **読み取り**: 名前画像に一致する過去の名前が無いとき、文字辞書で読む(V.readName)。
  1文字=1〜3塊としてDPで最良の区切りを選ぶ＝「け」「は」「ぬ」の縦棒や濁点が別塊でも正しく束ねる
  (塊をそのまま1文字とみなす方式は23プレート中14しか当たらない)
- **1文字は16x16 + セル重ね0.25**。24x24だと1セル≒1pxで平滑化が効かず、枠が1px違うだけで
  同じ字のNCCが0.98→0.77に落ちる(「り」の実例)。重ねを入れて同字1位正解45/45
- **しきい値0.90**: 実測で「辞書に無い文字の最良スコアは最大0.897」→ 知らない字は読まずに諦める
- **実測(同素材23プレート)**
  - 文字辞書OCRの leave-one-at-plate-out: **完全一致8 / 不一致0 / 読めず14**(読めず=その名前の字が辞書に無い)
  - 全字を学習済みで読み直し: **22/23完全一致・不一致0**(辞書81枚/58文字)
  - 画像シグネチャ照合(v0.3.13)はリファクタ後も 正解8/誤り0/取りこぼし0/正しい棄権15 で退行なし
- 自動入力された名前は辞書に収穫しない設計を維持 → **OCRの誤りが自分を強化することはない**。
  手で直せばその字が正しいラベルで入る(自己修復)
- **ダブルスの「自分の行」判定も刷新**（同v0.3.14）
  - 旧方式は欄全体のtextVecを同梱mynameテンプレと照合。実測4試合とも正解だったが
    **差が0.021〜0.064しかなく、外れた側が0.936〜0.979**（同じ3文字の味方名と紙一重）。
    さらに同梱テンプレは開発者の名前なので**他の利用者には最初から効かない**
  - 新方式: **シングルスVS画面の自分側プレート(R.myname)からシグネチャを自動学習**し
    (myname-user.json・直近3枚)、ダブルスの左列2行と照合して自分の行を決める。
    ユーザーが名前を入力する必要はなく、誰の環境でも自動で成立する
  - 実測: 自分=0.938〜0.949 / 味方=0.000〜0.231（左列は右寄せ・シングルスは左寄せ・欄サイズも
    違うが、インク枠の正規化で一致する）。処理順を再現した通し確認でも4/4正解
  - 自分の名前が未学習のとき（ダブルスだけのバッチの初回等）は旧方式へフォールバックし、
    **差が0.10未満なら自分/味方キャラを要確認**にする（従来は無警告で入れ替わりうる状態だった）

### v0.3.16(公開済み 2026-09-02) ゲーム画面の位置指定(配信レイアウト対応)＋バナーのリボン判定の修正
配信録画はゲームが画面いっぱいとは限らない(枠・カメラ・チャットが同居する)というユーザー要望。

- 設計: 「フレーム内のゲーム画面」を**割合(rx,ry,rw,rh)**で SETTINGS.gameRect に保存し、
  vision.js に `setSourceRect/getSourceRect/srcRect` を追加。**切り出しは全部そこ基準**
  (`frameToData`/`cropRegion`、index.html の `cropDataUrl`/`frameJpeg`/`regionJpeg`)。
  座標系は従来どおり1920x1080基準のままなので、認識ロジックは1行も変えていない。
  割合で持つので、同じOBSレイアウトなら1080p録画でも1440p録画でも同じ設定が使える
- UI: 「ゲーム画面の位置」パネル →「変更…」でモーダル。**ドラッグ選択／数値(px)入力／自動検出／
  16:9スナップ／切り出しプレビュー**。全ファイル共通の設定として保存
- 自動検出: 時間方向に動いている画素の範囲＝ゲーム画面(静止した枠・ロゴ・チャットは動かない)。
  **等倍で走らせるのが必須**(下記)。列/行の投影を「中央値の30%」でしきい値化して最長ランを取る
- **精度の勘所: ずれは解像度低下よりずっと効く**。合成テスト(1920x1080の中にゲーム1408x792=横73%)で
  | 指定 | コート | 自分キャラ | 相手キャラ |
  |---|---|---|---|
  | 全画面(元動画) | 0.984 | 0.999 | 0.963 |
  | 正確な矩形 | 0.988 | 0.993 | 0.957 |
  | **4pxずれた矩形** | **0.835** | **0.776** | **0.716** |
  → 縮小そのものの損失はほぼゼロ。**1px単位で合わせることが全て**なので、自動検出は等倍で行い、
  数値入力欄(OBSのソース位置をそのまま入れられる)を用意した
- 合成テスト全項目: scan=vs@10-13/winner@142-144(全画面と同一)・コート0.988・キャラ0.993/0.957・
  勝敗パネル t=144.75 row1・パネルアイコン0.993/0.930・勝者スコア7・**ラケット4件すべて0.966〜0.989で正解**
- テスト素材の作り方: CapCut同梱ffmpegで元動画を縮小し、静止背景＋チャット枠＋カメラ(動く)を合成
  (`samples/stream-layout-test.mp4`・生成コマンドは docs/game-screen-rect.md)
- 位置指定なしでこの動画を読ませるとイベント0件になるので、**VS画面が1件も無い場合は
  「ゲーム画面の位置を指定してください」と警告**するようにした

### v0.3.15(公開済み 2026-09-01 00:10・ダブルス解析が必ず落ちる緊急修正)
- v0.3.14のダブルス自分行判定リファクタ(10510bd)で `s1`/`s2` を `else` ブロック内の
  `let` にしたまま、ブロック外の低スコア判定 `if (Math.max(s1,s2) < 0.75)` を残していた。
  → **ダブルスを含む録画は解析が `ReferenceError: s1 is not defined` で必ず中断**
  (シングルスのみの録画では発生しない。実戦ログで発覚)
- 修正: 旧方式のスコアを `nameScore`(外側スコープ・sig判定時は1固定)に退避し、
  低スコアフラグはその値で判定。sig判定が成立したときは従来どおりフラグを立てない
- 教訓: 分岐の片方でしか定義されない変数を分岐外で参照していないか、
  リファクタ時は「ダブルスを1本通す」まで確認する(シングルスのみの確認では素通りする)

### v0.3.13(公開済み 2026-08-31・相手名の自動入力を刷新)
ユーザー報告「文字数や似た名前だと誤って補完される」の原因調査と対策。

- **原因**: 照合が名前欄(410x85)全体を32x8マスにした白塗り量の相関だった。2〜3文字の名前だと
  32列中10列ほどしか文字が無く、残りの「空白」が一致する＝**文字の形ではなく文字数と配置を見ていた**
  - ユーザー辞書(53人/64枚)の全ペア実測: **別人なのに0.90以上が20組・全て同じ文字数の短い名前**
    (ぱぱ↔りな=0.969 / ぱぱ↔かみ=0.964 / すー↔な～=0.960 / ゆあん↔だゆう=0.938)
  - 同一動画23プレートの実測: **同一人物 0.656〜0.670 / 別人 最大0.835 と逆転**。
    しきい値0.90は「同一人物には当たらず、短い名前同士でだけ誤爆する」位置にあった
  - ダブルスは左列が右寄せ・右列が左寄せ・欄サイズも違う(370x64)ため、同一人物の一致率も落ちていた
- **対策**: 照合シグネチャを **白文字のインク枠を切り出して48x12へ正規化** したものに変更(V.nameSig)
  - 明るいベタ帯(コート/UIの横線)は行ごと除外・文字帯はインク行の最も背の高い連続塊・
    列も塊化して最も幅の広い塊(プレート右端の細い光を除く)
  - **文字列の縦横比ゲート**(同一人物は実測で比1.0000・許容1.12)
  - **僅差ガード**: 異なる名前の2位と0.05差未満なら自動入力しない(キャラ/コート照合と同じ規則)
  - 引き継ぎ(propagateName)も同じシグネチャに統一 → ダブルスの左列⇔右列をまたいでも一致する
- **実測(同素材23プレート)**: 同一人物 0.984〜0.986 / 別人 最大0.325 の完全分離。
  leave-one-out(1枚を辞書から抜いて当てる)で **正解8/誤り0/取りこぼし0/正しい棄権15**
  (旧方式は同一人物が0.67で閾値に届かず正解0)
- 既存のユーザー辞書(旧形式)は**照合には使わない**(誤爆源のため)。名前候補プルダウンの履歴としては
  引き続き使う。新形式(s)は名前を手入力/修正してCSV保存するたびに貯まる
- 未対応: ダブルスの自分行判定(TPL.mynameとの照合)は旧方式(欄全体textVec)のまま。
  同じ3文字の味方名と僅差になりうる(すじえ↔ガメス=0.771の実例)。myname辞書の再収穫が必要

### v0.3.12(公開済み 2026-08-31・低ビットレート録画のレート取りこぼし+拡大表示の画像送り)
検証素材: samples/2026-08-29 00-08-01.mp4（38分・1.76GB=**約6.2Mbps** FHD・11試合/3部門混在）

- **確認UIの拡大表示に画像送りを追加**（ユーザー要望2026-08-31: パラパラ再生を拡大するとその1コマしか見えない）
  - パラパラ再生をクリック→**今映っているコマから開き**、←/→ か画面端の‹ ›で前後6コマを行き来。
    キャプションに「発動+0.4秒 3/6」と発動からの相対秒を表示。Space/ボタンで拡大したまま自動再生
  - 静止画も同じ試合カード内の根拠画像をまとめて送れる（キャプションは項目名「レート変動前 12/18」）
  - 拡大中は ←/→ を試合送りに渡さない。コマ画像は384→**640px**（拡大しても誰が撃ったか見える）
- **レート読みの second-chance パス**（この素材で22項目中9項目が空欄・1項目が無警告誤りだった）
  - 原因① 塗りの白にパネル色がにじんで彩度ガード(<15)に落ち、数字が丸ごと欠ける
    （「3,271」→「321」＝3桁で棄却・「3,422」→「342」）
  - 原因② 正しい4桁を読めているのに確信度が受理ライン0.75に届かない（「3,278」を**0.69で8フレーム安定読み**）
  - 原因③ 安定ランが1つしか取れないと「変動前の値を変動後として返す」
    （m10: 画面は3422→3446。カウント後の3446が0.71で棄却され**変動後=3422が無警告で入っていた**）
  - 対策: 通常読み(fillink/conf0.75)で前後がそろわなかったときだけ、緩和マスク **fillink2**
    (彩度ガード<46・閾値 p98*0.78。sat34では別サンプルが崩れ42以上が安全域と実測) と lightink で
    再走査し conf>=0.60 を採用。緩和読みは **weak=必ず要確認フラグ**（レート連鎖の起点にもならない
    ＝無警告の誤りは増やさない）。前後そろえば通常読みの単一ランより優先（原因③の根治）
  - 追加サニティ: 「変動前==変動後」「勝敗とレート増減の矛盾」→ 要確認
  - 実測: **22項目すべて充填・3部門ともレート連鎖が完全一致**
    (fever 3302→3271→3278→3292→3277 / classicS 3427→3410→3422→3446 / classicD 2525→2549→2569→2596→2605)
  - 副次効果: ダブルスは勝敗をレート増減で確定する設計のため、レートが直ると **スコア/result も復活**
    （m4=8-6 win・m11=7-2 win が丸ごと空欄→充填。パネル自体は conf0.95〜0.99 で読めていた）
- **ハテナ誤判定のガード**: 帰属できたイベントが2件（1種1件ずつ）ではハテナと断定しない。
  m9(ワルイージピンボール・フィーバー)はバナー12件中side帰属が2件だけで「相手=ハテナ」に化けたが、
  実際は **自分=かざん／相手=カーブ**（かざんショットの発動フレームで近側のハナチャンが撃っている）。
  ハテナ(ランダム発動)なら同じ側から3件以上出るので、total>=3 のときだけハテナ・それ未満はnull+フラグ
- **前試合ラケットの持ち越し抑止**: この試合で照合できたバナー名に前試合のラケットが1本も無ければ
  仮入力しない（m9でビリキューが入るが、この試合のバナーはかざん/カーブのみ＝ラケットを変えている）
- 既知の限界(未対応): ワルイージピンボールはHUD可視ゲートが通りにくく、FVゲージのside帰属が
  ほとんど付かない（m9=12件中2件・しかも1件は誤帰属）。パラパラ再生での目視が現状の回避策

### GT精度検証(2026-08-30・12Mbps推奨設定2本・9試合・回答=FEVER登録値)
- 検出9/9・**セル精度77/80=96.3%・無警告誤りゼロ**・完全一致6/9
- 不一致3件は全て辞書未収穫起因のフラグ付き(ワルイージ/ファイアバー/マジック)→即収穫済み
- レート連鎖3部門完璧・8-6正読・新コート グラス正読・回線全問正解

### 辞書収穫作戦 パイロット結果(2026-08-31・samples/2026-08-31 23-55-06.mp4)
ランクマ外(COM戦・アカデミー クレイ・ベビィピーチ vs ベビィワルイージ・7ポイントマッチ)で
「ランクマと同じ表示形式か」を検証。運用・詳細は docs/harvest-checklist.md。

- **VS画面/勝敗パネルはランクマと同一**: ランクマ由来の辞書がそのまま当たる
  (コート「アカデミー クレイ」0.984 / VSアイコン ベビィワルイージ 0.963 /
   locateWinner成立 t=144.75 row1 / パネルアイコン ベビィワルイージ 0.944 / 勝者スコア7 conf1.00)
  → **B/C/D(コート・キャラ)はオフライン録画のまま量産できる**
- レートパネルが無いので groupMatches は0件(=アプリのUIには出ない)。収穫は目視ラベルなので支障なし
- 回線アイコンが無い → HUD可視ゲートが通らず FVゲージの side帰属は不可(同上・支障なし)
- **ラケット発動バナーも形式は同一。落ちていたのはアプリ側のバグだった**(ユーザー指摘で判明)。
  当初「オフラインはキャプション形式で別物」と判定したが誤り。バナーのリボンは
  **ショットごとに色が違う**(ファイアバー=橙/マジック=青紫/**ドッスン=灰**/**ドロドロ=茶**)のに、
  findBannerのリボン条件が「高彩度(sat>80画素が15%以上)」だったため、
  **灰・茶リボンのラケットが丸ごと検出されなかった**(ドッスン実測 ribbonSat=0.000)。
  「ドッスンは画面端で見切れて収穫失敗」という過去の記録も**誤診**でこれが正体
  - 対策(v0.3.15候補): リボン判定を「高彩度 **or** 帯の輝度sd>=35 かつ 文字幅>=660」に緩和。
    低彩度側の誤検出はワルイージピンボールの得点メダル「Mario Tennis Fever」(幅475〜558)のみで、
    本物のバナーは最短でも幅701(7文字名)＝幅660で分離できる
  - **退行チェック: ランクマ素材(2026-08-30 01-44-03・30分)の通過イベント28件が前後で完全に不変**
    (バナー候補41件を彩度/輝度sd/幅/darkIn付きで全記録し新旧ゲートを突き合わせ)
  - 効果: パイロットの検出が **0件→4件**。同一ラケットの別発動どうしが **1.000で一致**
- 収穫: **ドッスン/ドロドロ** racketBanners 84→88本・19→**21種** ／
  **ベビィピーチ** vsIcons 98→101本・panelIcons 81→82本(収穫に使っていない別フレームで0.895〜0.930)。
  収穫前は勝敗パネルで「ピーチ」0.866＝**しきい値0.8を超えた無警告の取り違え**だった
- → **A/B/C/D すべてオフライン(COM戦)のまま量産できる**状態になった

### 辞書収穫作戦 A/B完了+C前半(2026-09-05・samples/2026-09-05 00-04-56.mp4 25分 + 00-35-18.mp4 6分40秒)
ユーザーが docs/harvest-checklist.md の A(ラケット9種)/B(コート2種)/C(キャラ)をCOM戦で録画。目視ラベルで収穫。
- **A 完了**: 未収穫9種(マイ/ファイア/サンダー/ビューゴー/ファイアフラワー/シャドウ/インク/ブル/キラー)を全部収穫
  → racketBanners **99本/30種**(ハテナ以外すべて)。同一ラケットの別発動どうしは全9種 0.999〜1.000
  - **白リボン対応(vision.js findBanner)**: フィーバーショット(白×灰縞)/ビューゴーショット(白×水色縞)はリボンが
    「明るい無彩色」で文字画素と区別できず、文字ボックスが帯いっぱい(幅1790)に膨らんでいた。
    「文字ボックスが帯の75%超」のときだけ「黒縁に両側を挟まれた白ラン」の厳格マスクで取り直す分岐を追加
    (幅660未満は棄却=帯左下の「Winner ガメス」が幅294で通った実例)。色リボンは分岐に入らないので
    **ランクマ素材(2026-08-30 01-44-03・30分)の検出35件が新旧で完全一致**(退行なし)
  - 収穫前は4種が既存ラケットに閾値0.93超で化ける状態だった(ファイア→アイス0.944／ファイアフラワー→アイスフラワー0.957／
    ブル→メタル0.960／キラー→スター0.932)。**無警告の誤名になりうる状態**が解消(本人が0.997〜1.000で勝つ)
  - **旧辞書の誤ラベル10本を除去**(samples/frames/cleanup-rackets-0905.js): カーブ×6・たつまき×2がトゲゾー(24本クラスタ)と
    1.000で同一、オシダシー×2がサンボ(11本クラスタ)と0.999で同一。2026-08-21の「side帰属×GT」自動収穫の汚染。
    同じ絵に別ラベルが付くと同点で先に並んだ方が無警告で勝つ。全テンプレ総当たりxcorrで検出(手順=samples/frames/*.js)
- **B 完了**: スタジアム クレイ(収穫前「スタジアム グラス」0.904で化けていた)/ラケットファクトリー(0.664=フラグ止まり)
  → courts 58本/**16種**(ランクマに出るコートは全部)。2本目の動画で スタジアム クレイ 0.982〜0.988 を確認
- **C**: 1本目は途中終了の試合が多く勝敗パネルは1試合のみ。2本目(00-35-18)は4試合完走。
  VS+勝敗パネル両方=ルイージ/ワリオ/ワルイージ/キノピオ/テレサ/カメック/ディディーコング。
  VSのみ=ワンワン/トッテン/ベビィマリオ/ベビィルイージ/クリボー/チコ(基本+緑赤青オレンジ黒水色)/
  ヨッシー(水色ピンク赤黄)/ヘイホー(緑水色ピンク黄)/ノコノコ(基本+青)/パタパタ(青緑)。
  → vsIcons **170本/57種**・panelIcons **95本/33種**。検証: 全VS/パネルフレームで本人が0.985〜1.000のtop1
  - 注意: 色違い同士はiconVec(RGB16x16のNCC)でも0.93〜0.95まで似る(ヨッシー赤↔オレンジ0.952等)。
    本人テンプレがあれば勝つが、アニメ差で入れ替わる余地はある(要注視・辞書を増やして吸収)
  - classify('winner')の偽陽性: コートセレクト/キャラセレクト画面と**サンダーショットの黄リボン**が'winner'に分類される
    (オフライン限定の画面は無害。サンダーショットはランクマでも出るがlocateWinnerの後段で棄却されるはず・未検証)
- 収穫の型(今回確立・samples/frames/): sweep-0905.js(全区間: バナー候補+VS+サムネ一覧)→grab-0905.js(証拠シート)→
  目視ラベル→findbanner-patch.js/icon-harvest-0905.js(ベクトル抽出+交差検証)→merge-0905.js(両templates.jsonへ)。
  video要素を複数作れば走査と収穫を並行できる(video2/3で0.1秒刻み全走査)。VS画面は0.2秒しか映らないことがある→0.1秒刻み必須。
  フェードイン中のフレーム(runの先頭)はベクトルが崩れるので落とす

### 辞書収穫作戦 C後半(2026-09-05・samples/2026-09-05 12-04-12.mp4 13分26秒)
ユーザーが「VSのみ組を最後まで」を上からチコまで録画(7試合全部パネル到達・VS画面も1秒以上)。目視ラベルで収穫。
- 収穫: ワンワン/ボスパックン/ほねクッパ/トッテン/ベビィマリオ/ベビィルイージ/チコ7色(基本・緑・赤・青・オレンジ・黒・水色)＋ディディーコング(別サイド)
  → vsIcons 170→**227本/57種**・panelIcons 95→**110本/46種**。試合表は docs/harvest-checklist.md「C 後半の結果」
- 検証: 収穫に使っていない別フレームで VS本人0.84〜1.00／パネル本人0.99〜1.00のtop1。収穫前はパネルで
  ワンワン→カメック0.60・トッテン→ワルイージ0.74・チコ→ノコノコ:赤0.82・チコ:オレンジ→ベビィピーチ0.82 等に化けていた
- **罠: ページの `video` 要素(メイン)はシーク後にステイルフレームを返すことがある**(t=152.7に飛ばしても直前の t=3.0 の絵が残る・
  makeSeekerでも防げず・冒頭の2フレームは前回位置800秒の勝敗パネルが混入して classify=winner になった)。
  収穫は **document.createElement('video') のフレッシュ要素**で行い、samples/frames/icon-harvest-c2.js は
  **2本の要素で順方向/逆方向に独立採取して ncc>=0.999 で一致したものだけ採用**する型にした(130点すべて一致・スキップ0)
- 全区間走査は samples/frames/sweep-c2.js(classifyのみ0.1秒刻み・4要素並列で13分動画が約6分)
- 辞書内の別ラベル総当たり(ncc>=0.90): VSは色違い同士のみ(**ノコノコ↔ノコノコ:青 0.985 は要目視確認**・チコ↔オレンジ0.955・
  ヨッシーオレンジ↔赤0.947)。パネルは ノコノコ:赤↔ノコノコ0.957・マリオ↔オーバーオール0.928・チコ↔オレンジ0.922・チコ青↔水色0.911
- アカデミー キノコ(7試合目)は辞書対象外のまま(ランクマに出ない)。アカデミー グラス0.889で最良一致=フラグ止まり

### 辞書収穫作戦 C後半②+D完了(2026-09-05・samples/2026-09-05 12-19-55.mp4 26分49秒 + 12-53-12.mp4 4分20秒)
- **C後半②**(シングルス11試合・全部パネル到達): ヨッシー8色/ヘイホー7色/ノコノコ:青/パタパタ青・緑・黄/クリボー をVS+パネルで収穫
  → vsIcons **305本/63種**・panelIcons **132本/63種**(残りは**ノコノコ:黄**だけ)。試合表は docs/harvest-checklist.md「C 後半②の結果」
- **D**(ダブルスVS表示9回): 35種+パタパタ基本 → dblIcons **100本/64種=全部**。別フレーム108アイコンで本人0.999〜1.000のtop1
- 検証(C後半②): パネル22フレーム全部本人top1(0.99〜1.00)。VSは27中21。外れ6件は全部**色違い同士の僅差**
  (ヨッシー オレンジ0.954↔黄0.957／パタパタ 黄0.867↔青0.915／ノコノコ 青↔赤0.910／スタジアムのヘイホー:黄→ピンク0.876=背景の写り込み)
- **構造的な限界(辞書では解けない・要アプリ側対策)**: ノコノコ/パタパタのVSアイコンは顔アップで甲羅が映らず色違いが
  **0.998**(パタパタ黄↔青)・0.985(ノコノコ↔青)まで一致。ヨッシー オレンジ↔黄0.975・ヘイホー 青↔水色0.967も僅差。
  現状 index.html のフラグは score<0.8 のみなので**色違いの取り違えは無警告**になりうる。
  ユーザー判断(2026-09-05): **色違い僅差でフラグは立てない**(毎回フラグが立つと登録者のチェックが面倒)。
  **背景除外は実装済み**: matchIcon(v, lib, len) に比較長を追加し、vsIcons は上11行(V.VS_ICON_LEN=528)だけで照合。
  辞書ベクトルは全体のまま(再収穫不要)。実測=辞書総当たりtop1 248→278/298・実フレーム160個 149→155・score<0.8フラグ1→0・
  別キャラ間max 0.873→0.849。残る外れは色違い同士のみ。panelIcons/dblIcons は領域がカード内に収まっているので対象外
- 全区間走査は sweep-c2.js(シングルス)/sweep-d.js(ダブルス・4アイコン+__harvestDbl 2要素独立採取)。証拠シートは __grabF(フレッシュ要素版)
- 旧辞書の点検(全テンプレ総当たり ncc>=0.93): vsIcons/panelIcons は色違い同士のみ。**dblIcons に ハナチャン↔ヨッシー:オレンジ 1.000**
  (2026-08の自動収穫の誤ラベル。タイル描画で青カードのオレンジヨッシーと確認)→ ハナチャン側の1本を両templates.jsonから除去(dblIcons 100→99本)

### 辞書カバレッジ(2026-09-05 C後半②+D時点)
- racketBanners **99本/30種**=ハテナ以外全部収穫済み(docs/rackets.md)
- courts 58本/**16種**=ランクマに出るコート全部(アカデミー アイス/キノコは**ランクマッチに出ないため対応不要**・ユーザー情報2026-08-30)
- vsIcons **305本/63/64種**・panelIcons **132本/63/64種**・dblIcons **99本/64/64種**。未収穫は **ノコノコ:黄(VS/パネル)のみ**。
  未収穫キャラはフラグで検知され教えるサイクル+同梱辞書移植で育つ。
  具体的な未収穫リストは「node -e」でtemplates.jsonのラベルとcharacters.jsのALLを突き合わせれば再生成できる

### samples/ の棚卸し(2026-09-05・v1.0.0公開後・ユーザー指示「使えるものはArchive・不要は削除・確認不要」)
- **samples/ に残す(ツール/ドキュメントが直接参照するGT・計3.7GB)**: 2026-08-20 10-44-35(砂クラシック・rally/highlightの主GT)・
  2026-08-20 22-18-37(クレイ赤・rally GT)・2026-08-21 01-57-16(720pダブルス・highlight GT)・2026-08-29 00-08-01(6.2Mbps FHD 11試合・highlight/精度GT)・
  stream-layout-test.mp4(配信レイアウト合成テスト)
- **Archive/ にZIP退避(9本・約10.8GB)**: 00-49-49(ラケット演出カタログ)・08-30 01-44-03(ランクマ30分・バナー退行チェック)・08-31 23-55-06(収穫パイロット)・
  09-05 00-04-56/00-35-18/12-04-12/12-19-55/12-53-12/13-18-43(辞書収穫A〜Dの元動画)。復元は zip を samples/ に展開
- **削除(参照なし・計34.5GB)**: 2026-08-29 01-25-46(23.8GB・48Mbps実戦12試合・v0.3.8で検証済み)・13-42-43・15-42-16・08-30 01-22-26
- samples/frames は証拠画像(png/jpg 270MB)を全削除し、収穫JSON(ベクトル)+スクリプト+templates.json だけ残した(18MB)。
  app/dist は 1.0.0 の3点セット以外(0.2.2〜0.4.0のexe・win-unpacked)を削除(2.2GB→113MB)。リポ直下の supabase/.temp(CLIの誤リンク)を削除
- 退避のやり方: 2GB超は Compress-Archive 不可 → C:/Windows/System32/tar.exe -a -cf Archive/NAME.zip NAME.mp4（mp4は圧縮されないので容器扱い・9本10.8GBで約3分）

### 残タスク
- ~~v1.0.0のビルド&発行~~ → **2026-09-05 公開済み**（https://github.com/games-desu/senseki-scan/releases/tag/v1.0.0）。教訓: package.json の改番はビルド開始前に済ませる（改番中にビルドが走り0.4.0版ができた→再ビルド）
- **辞書収穫作戦(進行中)**: ユーザーがdocs/harvest-checklist.mdを見ながらランクマ外で録画→samples/へ→Claudeが目視収穫してチェックリスト更新・コミット(確立済み運用2026-08-30)。
  **2026-09-05 ほぼ完了**: 残りはノコノコ:黄(VS/パネル)のみ。色違い僅差のフラグは立てない方針(ユーザー判断)・背景除外は実装済み
- v0.3.9のビルド&発行(FV帰属刷新)
- v0.3.6のビルド&発行（バグ報告機能・CSVコピー連携を含む）
- ~~リンクまとめサイトへの掲載~~（2026-09-05済み）・~~X公開告知~~（2026-09-06 ユーザーが投稿。旧URL版は削除して games-desu URLで再投稿）（v1.0.1は2026-09-05に発行済み）

### v0.3.0 に入っている機能
- 複数動画バッチ解析（D&D）／全部門自動識別（クラシックS/D・フィーバーS/D）
- 確認UI: **1件ずつ確認=照合ペア型**（証拠画像↔入力欄が行ごとに横並び）・**一覧=シアター型**・
  進捗ドット（クリックジャンプ・赤=要確認）・チップ行（要確認n件の項目列挙）
- スコア妥当性検証（有効最終スコアのみ・候補読み直し・[自動補正]表示）／スコア入力補完
- プルダウン+かな部分一致（キャラは正典全64種=基本38+色違い26・characters.js）
- 相手名の自動入力（名前画像照合・履歴プルダウン・設定でON/OFF）＋**名前の引き継ぎ**
  （入力名を同一名前画像の他試合へ自動転記。同ロビー連戦・味方⇔相手入れ替わり対応）
- 教えるサイクル（CSV保存時、手修正したキャラ/コート/名前をユーザー辞書へ収穫）
- リセットボタン・CSV保存ボタン上下2箇所・アプリアイコン・electron-updater
- **v0.3.6(未リリース)**: バグ報告機能（レポート保存=版数/環境/試合サマリー/解析ログのtxt・GitHub Issues/リンクまとめへの導線）・CSVコピー（クリップボード連携）・一括登録ページを開くボタン（open-external/save-text IPC追加）
- **v0.3.5(リリース済み)**: 砂コート全滅バグ修正（レートパネル検証ゲート=mode1照合0.85・winnerはrating到来まで上書き。10-44-35で3試合全項目GT一致）
- **v0.3.3/v0.3.4(リリース済み)**: レート/勝敗のGT修正（docs/rating-gt-check-2026-08-28.md=当て推量フォールバック廃止・panelIcons80・レートconf0.75・VSクロップのclassifyガード・1件ずつ確認の対戦カード/試合中を横並び化）＋ 回線品質の自動読み取り（readConnection=黄色列ランの本数・connClassic/connFever領域・試合中7回サンプル平均→CSV connection_quality列・HUD非表示は除外・任意項目なのでフラグなし。3モード実機フレームで検証済み）
- **v0.3.2(リリース済み)**: 試合中フレームの証拠画像(crops.midMatch・1件ずつ=対戦カード下/一覧=シアター)／自分ラケット不明時は直前フィーバー試合から仮入力([自動補正]+フラグ維持)／ シアターのラケット画像をmax-width560pxに制限／証拠画像は決定ラケットと同名のバナーを優先表示(racketCrop)／表示クロップにPAD16px(見切れ対策・認識には未使用)＋表示時findBannerもmaxH160／解析完了時にログ・バーを自動折りたたみ(「ログ」ボタンで再表示)／1件ずつ確認をコンパクト化(画像列400px・max-height230px)
- **v0.3.1(リリース済み)**: アップデートダイアログ（autoDownload=false・今すぐ/あとで/スキップ[updater.jsonに記憶]・Release説明欄を更新内容として表示・DL後は再起動/終了時適用を選択）
- **公開準備(2026-08-28)**: 初回ガイドオーバーレイ（初回起動時自動表示・「使い方」ボタンで再表示・settings.onboarded）・
  1件ずつ確認の ←/→ キー対応（入力欄内ではカーソル移動を優先）・利用者マニュアル docs/manual.md・READMEにダウンロード導線

### 認識エンジンの現況（2026-08-27の刷新後）
- スコア読み: **シングルス/ダブルス完全統一**＝枠内クリーン領域(dw1digit/dw2digit)+
  クリーン書体(panelFont収穫分∪ratingFont)+値最大採用+findPanelEnd。旧w1digit塊読みは引退
- レート読み: readRatingPair（パネル表示中を全走査し最初/最後の安定ラン=変動前/後）+
  カンマ無視+4桁固定+**レート連鎖補完**（変動前=同mode直前試合の変動後・矛盾は連鎖優先）+
  **試合前待機画面フォールバック**（連鎖起点なし時にvs手前45秒を走査）
- グループ化: groupMatches堅牢化（前試合のrating未到着中のvs窓を無視=試合中UIの誤分類対策）
- 設計原則: パネル送りはボタンで早められる（ユーザー情報）→ **固定時刻待ちは全廃し
  「実際に映っている区間を走査」**で読む

### 辞書（app/assets/templates.json = samples/frames/templates.json）
vsIcons 93 / dblIcons 54 / panelIcons 76 / panelFont 90 / ratingFont 214（新旧書体・0-9全桁）/
courts 47 / racketBanners 77 / myname 5 / mode 28 / albumBar 2

### 検証実績（GT=SENSEKI FEVER登録データとの突き合わせ）
| 検証 | 結果 |
|---|---|
| 2026-08-26実戦2本(FHD・GT18試合) | スコア17/18・result17/18・キャラ全枠一致・レート無警告誤りゼロ（誤り/欠けは全て要確認フラグ付き） |
| 1440pダブルス(GT8試合) | 8/8検出・スコア全取得・レート連鎖一致 |
| 720p/1440p/FHD過去検証 | 統一読みへの退行なしを確認済み |
| 2026-08-29 FHD実戦3部門混合11試合 | 検出11/11。ユーザーと共同で全項目検証→誤りは新コート2種/未収穫ラケット3種/dblIcons不足が主因(全てフラグ付きで無警告誤りは偽待機画面レートのみ→v0.3.7で遮断)。修正分を全収穫 |
| FHDフィーバーラケットGT照合(5試合) | 2026-08-27実施→ docs/racket-gt-check-fhd-2026-08-27.md。誤名の主因=未収穫バナーの閾値超え誤マッチ→アイスフラワー/メタル収穫(74→77)・斜めバナーmaxH対応・過剰フラグ精緻化。無警告誤りゼロ維持 |

残る既知の限界:
- バッチ先頭試合のレート変動前は、録画に試合前待機画面が無いとフラグ付き手入力
- フィーバーラケットのFVゲージside帰属の精度（誤帰属で相手ラケットが誤提案になる。フラグは付く。
  2026-08-29実戦で自分発動のバナー3件が全てopp帰属になる実例=ビリキュー1・かざん2。相手発動のopp帰属は正しかった）
- ワルイージピンボールの光るレールがfindBannerに誤検出される（サンボと0.92の偽マッチ実例。side=nullなので実害はフラグ止まり）
- フィーバーダブルス実戦・真の4K(3840)は未検証

### 運用ルール
- 検証済み動画は `samples/Archive/` へZIP退避（2GB超はbsdtar）。現在の残置=ラリー解析GT3本+ラケットカタログ1本
- 推奨録画設定（README記載）: 1080p以上・30fps以上・6Mbps以上・OBS元録画
- GT取得: match_recordsをservice_roleで照会（`npx supabase projects api-keys --project-ref bltnmpwgbmohvrqwtzmn`・my_name=ガメス）

## 2. ラリー解析（tools/rally.js, court.js, ball.js, rally-lab.html）

**着手時は必ず `docs/rally-analysis.md` を最初に読む**（現状・実測で確定した設計制約・16本の調査レポート索引）。
ゲーム仕様は `docs/game-mechanics.md`。今回のセッションでは変更なし（最終更新2026-08-25）。

| Phase | 内容 | 状態 |
|---|---|---|
| A | 得点HUD→ポイント境界 | 完成・検証済 |
| B | 画面⇔コート座標（ホモグラフィ・3自由度） | 完成・検証済 |
| C | ボール追跡→着弾点 | merged2が実用形（12ラベル12/11/12・静止物棄却再設計済み・**未本採用**） |
| D | トレイル色→ショット種別6種 | 未着手（仕様確定済み） |
| E | 可視化（ショットチャート） | 最小限 |

次の一手: **ラリーゲート**（ラリー外の誤検出を切る）→ merged2本採用 → Phase D。
検証素材: `samples/` 残置の 10-44-35（砂）・22-18-37（クレイ赤）・01-57-16（石畳）。

## 3. ドキュメント索引

| ファイル | 内容 |
|---|---|
| docs/STATUS.md | このファイル（全体ステータス） |
| docs/screens.md | 画面仕様・抽出座標の正 |
| docs/new-screens-wip-2026-08-27.md | 実戦フィードバック対応の完了報告（画面バリエーション問題の全記録） |
| docs/dbl-winner-panel-wip-2026-08-26.md | 勝敗パネル対応の完了報告 |
| docs/ui-feedback-2026-08-26.md | 確認UI刷新の記録 |
| docs/rackets.md | フィーバーラケット対応表 |
| docs/game-screen-rect.md | ゲーム画面の位置指定（配信レイアウト対応）の設計・精度・テスト素材の作り方 |
| docs/harvest-checklist.md | 辞書収穫作戦のチェックリスト（ユーザー作業用） |
| docs/release.md | ビルド・リリース手順（updater配信） |
| docs/highlight.md | ハイライト生成（得点シーン切り抜き・名前バナー除外・ffmpeg同梱）の設計と検証 |
| docs/csv-accuracy-*.md | 各精度検証の詳細 |
| docs/rally-analysis.md | ラリー解析の正（着手前必読） |
| docs/game-mechanics.md | ゲーム仕様（ユーザー直伝） |
